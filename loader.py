"""Person 1's proposed area: BasicML file loading and validation."""


def load_program(path: str) -> list[int]:
    """Return validated words in file order; do not mutate the machine.

    TODO: read the file, validate signed four-digit words and the 100-word
    limit. Agree on whitespace/blank-line handling before implementation.
    Use OSError for file access failures and ValueError for invalid content.
    """
    raise NotImplementedError("File loading is not implemented yet.")
